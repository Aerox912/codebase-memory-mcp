# Aerox 0.10.8-aerox.7 snapshot

Integrates canonical `DeusData/codebase-memory-mcp` through
`ffb5ebe9e8ad2763fc6388c001ca8a16ca834d88`, retaining the maintained Aerox
Windows amd64 and Linux amd64 release packaging. This is a snapshot beyond
canonical v0.10.8, not a new canonical release.

Changes include Swift calls, Razor/CSHTML discovery, TypeScript import
resolution, Cypher behavior, daemon version cohorts, and platform handling.
The release assets are rebuilt from this merge; checksums cover both archives
and the Windows MCPB bundle. Existing graph stores remain platform-separated.
